package structure;

/* Tentative de création d'une interface pour faire passer une fonction 
 * en paramètre afin de factoriser du code (voir IA.getVoisin).
 */
public interface Verificateur {
    boolean verifie(Point p);
}
